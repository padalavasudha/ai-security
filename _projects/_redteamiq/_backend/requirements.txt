# RedTeamIQ backend dependencies
# Web layer
Flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Microsoft Foundry — Agent Service (2.0.0 surface) + auth + OpenAI-compatible client
azure-ai-projects>=2.0.0
azure-identity>=1.19.0
openai>=1.50.0

# HTTP client for calling real target agent endpoints + the demo targets
requests>=2.31.0
